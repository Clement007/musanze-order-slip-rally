const qty = document.getElementById("qty");
const price = document.getElementById("price");
const total = document.getElementById("total");

// RELAY:
// Student 1 → Check if elements exist before using them
// Student 2 → Add input event listener
// Student 3 → Convert values to numbers
// Student 4 → Display live total
// Student 5 → Prevent negative values

console.log("JS connected");